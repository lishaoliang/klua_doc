# kmnp

> `require("kmnp")` — 代码: `klb/src_c/klua/klua_net/klua_kmnp.c`

**现状**: `klua_open_kmnp` 仅注册空模块表, **无** 导出函数. MNP 载荷规范见 **klb-mnp-smp-design**; 连接能力见 `ksmp` / `khttp_mnp` 等.

**对称**: 无 `klbcore.klbmnp` 脚本包.
