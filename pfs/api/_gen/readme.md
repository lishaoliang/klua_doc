# 生成 API 文档

> `klua_doc/pfs/api/_gen/`

**勿手改.** 由 `.tmp/pfs-api-docs/gen-api-docs.ps1` 生成 (迁移后输出路径改为此目录).

```yaml
generated: true
do-not-edit: true
```

同步命令: 用户说 **同步 api 文档** / **更新 api 文档** 时执行脚本.
